html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/style.css', type='text/css')
  body
    
    h1= header 
    ul
        each val in messages
            li= `${val.slackUserId} feels ${val.responseText}`